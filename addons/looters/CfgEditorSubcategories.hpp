class CfgEditorSubcategories {
    @For(Identities)
        class CLASS($1_Looters_Arid) {
            displayName = "Men (Arid, $2)";
            GCLASS(identity) = "$1";
        };
        class CLASS($1_Looters_Boreal) {
            displayName = "Men (Boreal, $2)";
            GCLASS(identity) = "$1";
        };
        class CLASS($1_Looters_Cold) {
            displayName = "Men (Cold, $2)";
            GCLASS(identity) = "$1";
        };
        class CLASS($1_Looters_Desert) {
            displayName = "Men (Desert, $2)";
            GCLASS(identity) = "$1";
        };
        class CLASS($1_Looters_Tropic) {
            displayName = "Men (Tropic, $2)";
            GCLASS(identity) = "$1";
        };
    @EndFor
};
